# Example environment variables for local development
# Provide comma-separated values when multiple entries are required.

VITE_ADMIN_EMAILS=fenilmuneer@gmail.com
VITE_ADMIN_PASSWORDS=vpm@2522

# Do not commit real secrets. Use repository secrets or CI variables for production deployments.
